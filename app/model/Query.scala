package model

import java.io.{File, FileInputStream, ObjectInputStream}

import LSH.hashFunctions.{CrossPolytope, Exhaustive, Hyperplane}
import LSH.structures.{HashTable, LSHStructure}
import tools.Cosine
import IO.Parser

import scala.collection.mutable.ArrayBuffer
import scala.util.Random

/**
  * Created by remeeh on 02-11-2016.
  */

object Query {
  // Load LSHStructure
  // TODO Get path
  val ois = new ObjectInputStream(new FileInputStream("out/structures/Hyperplane_12_4.lshstructure"))
  println("Loading started!")
  val lshs: LSHStructure = ois.readObject.asInstanceOf[LSHStructure]
  ois.close()
  println("Structure Loaded")

  // return the result generated by
  def getResults(imageId:String, range:Double) : (String, List[((String, IndexedSeq[Float]), Double)]) = {

    val fVector = lshs.findVectorById(imageId)
    val tres = time {
      lshs.query(fVector, range, Cosine)
    }

    val res2 = tres._1.zip(tres._1.map(x => Cosine.measure(fVector._2, x._2))).sortBy(_._2)
    (tres._2, res2.toList)

  }

  def time[R](block: => R) = {
    val t0 = System.currentTimeMillis()
    val result = block    // call-by-name
    val t1 = System.currentTimeMillis()
    (result, ((t1 - t0) + "ms"))
  }

}
